spring:
  datasource:
    url: ${AUTH_SVC_POSTGRES_URL}
    username: ${AUTH_SVC_POSTGRES_USERNAME}
    password: ${AUTH_SVC_POSTGRES_PASSWORD}
    driver-class-name: org.postgresql.Driver
api:
  server:
    deployed: ${AUTH_SERVER_URL}
